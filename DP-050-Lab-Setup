# Download and Prepare Lab Files

1. [] On @lab.VirtualMachine(DP050-SQL2008R2).SelectLink click @lab.CtrlAltDelete to to activate the Ctrl + Alt + Delete sequence and bring up the logon page.
	
	>[!KNOWLEDGE] Any links like the one above will send Ctrl+Alt+Delete to the selected machine. This can also be done the **Commands** menu (lightning bolt) in the upper-left hand corner of the screen.

1. [] Sign in as +++@lab.VirtualMachine(DP050-SQL2008R2).Username+++ with the password +++@lab.VirtualMachine(DP050-SQL2008R2).Password+++.

1. [] @[Download lab files to D: Drive][DownloadFromGit]{powershell}

>This will copy the latest lab files from Github. You will not see any output on screen.

[DownloadFromGit]:
```
git clone "https://github.com/MicrosoftLearning/DP-050-Migrating-SQL-Workloads-to-Azure" "D:\DP-050_LabFiles"
```

===
